# Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# Backend API URL (for server-side API calls)
# Use 'http://backend:8987' for Docker internal network
# Use 'http://localhost:8987' for local development
BACKEND_URL=http://backend:8987

# Public API URL (for client-side API calls from browser)
NEXT_PUBLIC_API_URL=http://localhost:8987

# Admin Authentication (REQUIRED for admin access)
# ⚠️ BREAKING CHANGE: Admin pages now require authentication
# This MUST match the ADMIN_API_KEY in the backend .env file
ADMIN_API_KEY=change-this-to-match-backend-key

# Session Secret for signing cookies (REQUIRED in production!)
# Generate a random string (at least 32 characters)
# To generate: openssl rand -hex 32
# ⚠️ App will throw error in production without this!
SESSION_SECRET=generate-random-string-here-at-least-32-chars

# ============================================
# IMPORTANT NOTES:
# 1. This file should be named .env.local (not .env)
# 2. Never commit .env.local to version control
# 3. ADMIN_API_KEY must match the backend exactly
# 4. SESSION_SECRET should be unique and random (REQUIRED in production)
# 5. See docs/guides/ADMIN_AUTH_MIGRATION.md for details
# 
# SECURITY FEATURES (automatically enabled):
# - Rate Limiting: 5 failed logins = 1 hour block
# - Session Expiry: 24 hours automatic logout
# - Session Cleanup: Memory management every hour
# - Security Logging: All auth events tracked with IP
# - Token Rotation: New token on each login
# ============================================